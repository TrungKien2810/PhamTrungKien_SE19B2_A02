<Window x:Class="PhamTrungKienWPF.CustomersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer Management" 
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="{DynamicResource SurfaceAltBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                          Text="Customer Management" 
                          FontSize="20" 
                          FontWeight="Bold" 
                          Foreground="White"/>
                
                <Button Grid.Column="1" 
                       Content="Close" 
                       Style="{DynamicResource DangerButton}" 
                       Padding="15,5" 
                       Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search and Add -->
            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox x:Name="SearchTextBox" 
                            Grid.Column="0" 
                            Height="35" 
                            Padding="10" 
                            VerticalContentAlignment="Center"
                            TextChanged="SearchTextBox_TextChanged"/>
                    
                    <Button x:Name="AddButton" 
                           Grid.Column="1" 
                           Content="Add Customer" 
                           Height="35" 
                           Width="120" 
                           Margin="10,0" 
                           Style="{DynamicResource AccentButton}" 
                           Click="AddButton_Click"/>
                    
                    <Button x:Name="RefreshButton" 
                           Grid.Column="2" 
                           Content="Refresh" 
                           Height="35" 
                           Width="80" 
                           Style="{DynamicResource PrimaryButton}" 
                           Click="RefreshButton_Click"/>
                </Grid>
            </Border>

            <!-- Data Grid -->
            <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" CornerRadius="5" Padding="10">
                <DataGrid x:Name="CustomerDataGrid" 
                         AutoGenerateColumns="False" 
                         CanUserAddRows="False" 
                         CanUserDeleteRows="False"
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="50"/>
                        <DataGridTextColumn Header="Full Name" Binding="{Binding CustomerFullName}" Width="*"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" Width="*"/>
                        <DataGridTextColumn Header="Telephone" Binding="{Binding Telephone}" Width="120"/>
                        <DataGridTextColumn Header="Birthday" Binding="{Binding CustomerBirthday, StringFormat=dd/MM/yyyy}" Width="100"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Edit" 
                                               Width="50" 
                                               Height="25" 
                                               FontSize="10" 
                                               Margin="2" 
                                               Style="{DynamicResource WarningButton}" 
                                               Click="EditButton_Click"/>
                                        <Button Content="Delete" 
                                               Width="50" 
                                               Height="25" 
                                               FontSize="10" 
                                               Margin="2" 
                                               Style="{DynamicResource DangerButton}" 
                                               Click="DeleteButton_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{DynamicResource FooterBrush}" Padding="10">
            <TextBlock x:Name="StatusText" 
                      FontSize="12" 
                      HorizontalAlignment="Center" 
                      Foreground="{DynamicResource TextSecondaryBrush}"/>
        </Border>
    </Grid>
</Window>
